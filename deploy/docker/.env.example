# Database
POSTGRES_DB=greengovrag
POSTGRES_USER=postgres
POSTGRES_PASSWORD=your_secure_password_here

# Vector Store
# Options: faiss, qdrant, chromadb
VECTOR_STORE_TYPE=qdrant
QDRANT_URL=http://qdrant:6333
# Qdrant API Key (generate with: openssl rand -base64 32)
# SECURITY: Change this in production! Use GitHub Secrets for cloud deployments
QDRANT_API_KEY=

# Embedding Model (used for vector indexing)
# Options:
#   - BAAI/bge-large-en-v1.5 (1024 dims, 93% accuracy, slower but best quality)
#   - sentence-transformers/all-mpnet-base-v2 (768 dims, 90% accuracy, balanced)
#   - sentence-transformers/all-MiniLM-L6-v2 (384 dims, 85% accuracy, fast)
EMBEDDING_MODEL=BAAI/bge-large-en-v1.5

# ===========================================================================
# LLM Configuration - Azure OpenAI (Recommended)
# ===========================================================================
LLM_PROVIDER=azure
LLM_MODEL=gpt-4o

# Azure OpenAI Credentials
AZURE_OPENAI_API_KEY=your-azure-api-key
AZURE_OPENAI_ENDPOINT=https://your-resource-name.openai.azure.com/
AZURE_OPENAI_DEPLOYMENT=gpt-4o
AZURE_OPENAI_API_VERSION=2024-12-01-preview

# Embedding Model (Free, Local) - Requires 2GB RAM
EMBEDDING_MODEL=BAAI/bge-large-en-v1.5

# ===========================================================================
# Alternative LLM Providers (Uncomment to use)
# ===========================================================================
# AWS Bedrock
# LLM_PROVIDER=bedrock
# AWS_ACCESS_KEY_ID=AKIA...
# AWS_SECRET_ACCESS_KEY=...
# AWS_REGION=us-east-1
# BEDROCK_MODEL_ID=us.amazon.nova-pro-v1:0

# OpenAI Direct
# LLM_PROVIDER=openai
# OPENAI_API_KEY=sk-your-key-here
# LLM_MODEL=gpt-4o

# Cache Settings (Production)
ENABLE_CACHE=true                          # Enable LLM response caching
ENABLE_REDIS_CACHE=true                    # Enable Redis for production
REDIS_HOST=redis                           # Redis container name
REDIS_PORT=6379
CACHE_TTL=3600                             # 1 hour cache TTL
ENABLE_SEMANTIC_CACHE=true                 # Enable semantic similarity matching

# CORS (for production)
CORS_ORIGINS=https://yourdomain.com,https://www.yourdomain.com

# Environment
APP_ENV=production
DEBUG=false
LOG_LEVEL=INFO
